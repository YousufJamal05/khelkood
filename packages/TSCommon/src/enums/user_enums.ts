/**
 * User roles available in the KhelKood platform.
 */
export enum UserRole {
  PLAYER = 'player',
  OWNER = 'owner',
  ADMIN = 'admin',
}

/**
 * User account status.
 */
export enum UserStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
}
